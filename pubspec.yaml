name: canon
description: "Application runtime context from a single grammar tree: navigation, identity, and state as one compile-safe spec."
version: 0.20.0
repository: https://github.com/CardosoShlomo/canon

environment:
  sdk: ^3.12.2

dependencies:
  canon_codec: ^0.1.3
  regent: ^0.0.1
  meta: ^1.18.0
  web: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
