name: canon
description: "Compile-safe Flutter navigation from a single grammar tree."
version: 0.16.0
repository: https://github.com/CardosoShlomo/canon

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  canon_codec: ^0.1.3
  flutter:
    sdk: flutter
  meta: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.31.0
